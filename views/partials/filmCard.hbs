{{#with .}}


<a class="card" href="/film?id={{id}}">
<div class="ageRating">{{ageRating}}+</div>
    <img src="{{poster.url}}" class="card__poster" alt="" srcset="">
    <span class="card__name">{{name}}</span>
</a>

{{/with}}