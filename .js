let pagination = (list) => {
    switch (list) {
        case 'top250':
            let re = 250/10;
                
            break;
    
        default:
            break;
    }
}
pagination('top250')